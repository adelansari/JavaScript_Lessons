/* 
- Write a function that takes two numbers as parameters and returns their sum.
- Use function keyword and the arrow function
 */

function sum(a, b) {
  return a + b;
}

const sum = (a, b) => a + b;
