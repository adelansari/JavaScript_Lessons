// Define a variable called count with an original value of 0 and then increment it on the following line

let count = 0;
console.log(`Initial value of count is $ `);
count += 1;
console.log(`Incremented value of count is $ `);
